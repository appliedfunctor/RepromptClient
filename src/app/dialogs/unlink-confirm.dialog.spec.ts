import { UnlinkConfirmDialog } from "app/dialogs/unlink-confirm.dialog";

describe('UnlinkConfirmDialog', () => {
    
    let component: UnlinkConfirmDialog = new UnlinkConfirmDialog()

    it('should create', () => {
        expect(component).toBeTruthy()
    })
})